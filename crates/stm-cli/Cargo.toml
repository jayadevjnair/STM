[package]
name = "stm-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
stm-core = { path = "../stm-core" }
stm-writer = { path = "../stm-writer" }
stm-parser = { path = "../stm-parser" }