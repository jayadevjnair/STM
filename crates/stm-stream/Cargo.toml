[package]
name = "stm-stream"
version = "1.1.0"
edition = "2021"

[dependencies]
stm-core = { path = "../stm-core" }
stm-crypto = { path = "../stm-crypto" }
sha2 = "0.10"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.10"
